# Dependencies for run_inference_ood.py — minimal portable set.
# Tested on Python 3.11. Install via:  pip install -r inference_requirements.txt
torch>=2.0
torchvision>=0.15
torch_geometric>=2.4
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
tifffile>=2024.1
imagecodecs>=2024.1     # required by tifffile to decode JPEG-tiled WSI .tif/.svs
zarr>=3.0,<4
matplotlib>=3.7         # only for optional viz; safe to omit on inference-only nodes
